<h1>VAT Calculator – Intra-EU Transactions</h1>

<p>Use this form to simulate VAT for a cross-border sale within the European Union.</p>

<%= form_with url: vat_simulator_calculate_path, method: :post, data: { turbo: false }, local: true do |f| %>
  <div>
    <%= label_tag :origin_country, "Origin Country" %><br>
    <%= select_tag :origin_country, options_from_collection_for_select(@countries, :code, :name), prompt: "Select a country" %>
  </div>

  <div>
    <%= label_tag :destination_country, "Destination Country" %><br>
    <%= select_tag :destination_country, options_from_collection_for_select(@countries, :code, :name), prompt: "Select a country" %>
  </div>

  <div>
    <%= label_tag :product_category, "Product Category" %><br>
    <%= select_tag :product_category, options_for_select(["books", "food", "other"]), prompt: "Select a category" %>
    <small>Only “books” and “food” may be eligible for reduced VAT rates.</small>
  </div>

  <div>
    <%= label_tag :sale_value, "Sale Value (€)" %><br>
    <%= number_field_tag :sale_value, nil, step: 0.01, required: true %>
  </div>

  <br>

  <%= submit_tag "Calculate VAT", class: "button" %>
<% end %>
